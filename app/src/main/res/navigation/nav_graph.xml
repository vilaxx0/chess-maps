<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mapsFragment">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.vbuc.firebasecruid.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <fragment
        android:id="@+id/mapsFragment"
        android:name="com.vbuc.firebasecruid.MapActivity"
        android:label="fragment_maps"
        tools:layout="@layout/activity_map" />
    <fragment
        android:id="@+id/clockFragment"
        android:name="com.vbuc.firebasecruid.ClockActivity"
        android:label="fragment_clock"
        tools:layout="@layout/clock_activity" />
</navigation>